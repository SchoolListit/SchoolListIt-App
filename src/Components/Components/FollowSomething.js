import React from 'react';
import { Typography } from '@material-ui/core';



export default function FollowSomething() {
    return (
        <React.Fragment>
            <Typography>It Takes A Village</Typography>
            <Typography>We believe in public education, and that education should be public. Parents need as much help as they can get. Easy access to what thier children are learning is the first step in partnering teachers. Find your schools and classes to see what's going on. </Typography>
        </React.Fragment>
    )
}
